services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      OUTPUT_PATH: /app/output
      UPLOAD_PATH: /app/upload
    volumes:
      - ./output:/app/output
      - ./upload:/app/upload
    restart: unless-stopped
